info:
  name: find all ingredients
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/ingredients"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
