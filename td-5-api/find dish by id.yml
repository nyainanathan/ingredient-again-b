info:
  name: find dish by id
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/dishes/10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
