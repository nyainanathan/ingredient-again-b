info:
  name: Attach or detach ingredients
  type: http
  seq: 6

http:
  method: PUT
  url: "{{url}}/dishes/10/ingredients"
  body:
    type: json
    data: |-
      [
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
