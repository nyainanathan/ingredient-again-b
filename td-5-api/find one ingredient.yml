info:
  name: find one ingredient
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/ingredients/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
